---
description: Discover/count Android projects, let user select one or multiple, then scan and fix (SDK/AGP/manifest/ProGuard/Billing/16KB)
---

Follow the `gamebox-scan-fix` skill exactly: discover projects, show the count/list, let the user select one or multiple projects, run Phase 0 policy check, scan only the selected projects, then fix one project at a time with user approval. Ask the user for the parent folder each time.

Extra context from user: $ARGUMENTS
