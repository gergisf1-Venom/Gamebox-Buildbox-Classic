---
description: Re-verify fixes and build unsigned test APK for real-device testing
---

Follow the `gamebox-test-apk` skill exactly: re-verify every fix, build the unsigned test APK to `./APK_AAB/[GameName]/`, and guide real-device testing with crash diagnosis. Ask the user for missing inputs each time.

Extra context from user: $ARGUMENTS
