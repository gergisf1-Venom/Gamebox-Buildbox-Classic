---
description: Check machine env for Buildbox Android builds (JDK/SDK/NDK/Gradle/emulator + Play requirements)
---

Follow the `gamebox-env` skill exactly: verify the machine setup and write `./Reports/_ENV/ENV_report.md`. Ask the user for any missing inputs each time.

Extra context from user: $ARGUMENTS
