// gamebox-buildbox-classic — OpenCode plugin entry.
// The 6 skills + 6 commands ship as markdown in ./skills and ./commands;
// this module only registers the plugin so OpenCode loads the package.
export const GameboxPlugin = async () => {
  return {};
};
